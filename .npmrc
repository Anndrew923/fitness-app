# ⚡ V6.21: Netlify 部署配置 - npm 認證
# 使用環境變數 ${NPM_TOKEN}，實際 token 在 Netlify 環境變數中設置
# 這樣可以避免將 token 提交到代碼庫

# 配置 npm registry 認證（用於 scoped packages）
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# ⚡ V6.22: 允許安裝不兼容的 peer dependencies（用於 @daniele-rolli/capacitor-google-auth）
# 這個包需要 Capacitor 7，但我們使用 Capacitor 6
# 對於 Web 構建（Netlify），這個包不會被實際使用，所以可以安全地忽略 peer dependency 警告
legacy-peer-deps=true
