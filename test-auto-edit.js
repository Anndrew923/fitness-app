// 測試自動修改功能
// 這是一個測試檔案，用於驗證 Cursor 的自動修改設定

function testFunction() {
  // 測試註解
  const message = "Hello World";
  console.log(message);
  
  // 測試變數
  let count = 0;
  count++;
  
  return count;
}

// 測試物件
const testObject = {
  name: "test",
  value: 123
};

export default testFunction;
